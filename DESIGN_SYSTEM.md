# CUBE Design System

> **브랜치**: `claude/cyberpunk-ui-design-ihQpL`
> **목적**: 디자인 컴파일 기준서 - main 코드 적용 시 유일한 시각 규칙 소스
> **최종 수정**: 2026-02-05

---

## 1. 브랜드 / 컨셉 선언

### CUBE 컨셉

**"지도 위에 떠 있는 관제 홀로그램"**

CUBE는 물류 데이터를 시각화하는 **관제(HUD) 인터페이스**다. 사용자는 마치 우주선 조종석이나 미래 도시의 통합 관제센터에서 물류 네트워크를 내려다보는 듯한 경험을 한다.

#### 핵심 감성 키워드
- **관제(Command & Control)**: 데이터를 모니터링하고 제어하는 시스템 인터페이스
- **홀로그램(Holographic)**: 반투명하게 떠 있는 패널, 지도 위에 오버레이되는 UI
- **네트워크(Network)**: 연결선, 노드, 경로가 시각적으로 드러나는 시스템
- **모듈(Modular)**: 독립적인 정보 패널들이 조합되어 하나의 대시보드를 구성

#### 시각적 레퍼런스
- 공상과학 영화의 함선 브릿지 인터페이스
- 항공 관제 시스템의 레이더 화면
- 게임 UI의 HUD(Heads-Up Display)
- 사이버펑크 장르의 네온 조명과 홀로그램

---

## 2. 컬러 시스템

### Primary Palette

| 역할 | 색상값 | 용도 |
|------|--------|------|
| **배경 (Base)** | `rgba(10, 10, 15, 0.85)` | 패널 배경, 반투명 오버레이 |
| **표면 (Surface)** | `rgba(22, 22, 32, 0.8)` | 카드, 입력 필드 배경 |
| **경계선 (Border)** | `rgba(0, 240, 255, 0.6)` | 외곽선, 구분선 |
| **포인트 (Accent)** | `#00F0FF` | 로고, 강조 요소, 글로우 |
| **텍스트 (Primary)** | `#FFFFFF` | 주요 텍스트 |
| **텍스트 (Secondary)** | `rgba(255, 255, 255, 0.6)` | 보조 텍스트, 설명 |

### Semantic Colors

| 상태 | 색상값 | 용도 |
|------|--------|------|
| **성공 (Success)** | `#10B981` (emerald-500) | 입도 경로, 완료 상태 |
| **경고 (Warning)** | `#F59E0B` (amber-500) | 주의 필요 상태 |
| **오류 (Error)** | `#EF4444` (red-500) | 실패, 위험 상태 |
| **정보 (Info)** | `#A855F7` (purple-500) | 출도 경로 |

### Marker Colors

| 타입 | 색상값 | 용도 |
|------|--------|------|
| **공간 상품** | `#FF6B35` (주황) | 창고/보관 공간 마커 |
| **도내 경로** | `#FFFFFF` (흰색) | 제주 내부 운송 |
| **입도 경로** | `#10B981` (초록) | 외부→제주 운송 |
| **출도 경로** | `#A855F7` (보라) | 제주→외부 운송 |

### 컬러 사용 원칙

- **청록(Cyan)은 시스템 요소에만**: 경계선, 글로우, 로고 등 "인터페이스 자체"를 나타내는 요소
- **흰색은 콘텐츠에**: 사용자가 읽어야 하는 모든 텍스트와 아이콘
- **채도 있는 색상은 의미 전달에**: 상태, 경로 유형 등 정보 구분이 필요할 때만 사용

---

## 3. 표면(패널) 규칙

### 보더 스타일

| 요소 | 두께 | 스타일 |
|------|------|--------|
| **외곽 패널** | 3px | `solid rgba(0, 240, 255, 0.6)` |
| **내부 구분선** | 1-2px | `solid rgba(0, 240, 255, 0.3)` 또는 `rgba(255, 255, 255, 0.5)` |
| **입력 필드** | 2px | `solid rgba(255, 255, 255, 0.5)` |

### 글로우(Glow) 원칙

- **사용 대상**: 로고, 활성 상태 버튼, 중요 액션 요소
- **강도**: `0 0 8px ~ 20px` 범위의 box-shadow 또는 filter: drop-shadow
- **색상**: 포인트 컬러(#00F0FF)의 투명도 변형
- **과용 금지**: 글로우는 "활성/강조" 상태에만, 기본 상태에서는 최소화

```
예시: 로고 글로우
filter: drop-shadow(0 0 8px rgba(0, 240, 255, 0.6))

예시: 버튼 글로우
box-shadow: 0 0 20px rgba(0, 240, 255, 0.3)
```

### 블러(Backdrop) 원칙

- **사용 대상**: 지도 위에 오버레이되는 모든 패널
- **강도**: `backdrop-filter: blur(8px)`
- **목적**: 홀로그램처럼 "떠 있는" 느낌, 배경 지도가 은은하게 비침

### 모서리 스타일

- **기본**: 직각(sharp corners) - 관제 시스템의 정밀함 표현
- **예외**: 버튼, 뱃지 등 인터랙티브 요소에 한해 `rounded-lg` (8px) 허용
- **금지**: 과도한 라운드(`rounded-full` 등)는 아바타/상태 인디케이터에만 제한적 사용

---

## 4. 타이포 규칙

### 폰트 패밀리

| 용도 | 폰트 | 비고 |
|------|------|------|
| **기본 텍스트** | Pretendard | 한글/영문 범용 |
| **숫자/데이터** | Inter | 고정폭 숫자, 가독성 |
| **시스템 표시** | monospace | 시간, 코드, ID |

### 텍스트 위계

| 레벨 | 크기 | 굵기 | 용도 |
|------|------|------|------|
| **H1 (타이틀)** | text-lg (18px) | bold | 섹션 제목 |
| **H2 (서브타이틀)** | text-sm (14px) | semibold | 카드 헤더, 라벨 |
| **본문** | text-sm (14px) | normal | 일반 텍스트 |
| **캡션** | text-xs (12px) | normal | 보조 설명, 힌트 |
| **데이터 (강조)** | text-xl (20px) | bold | KPI 수치 |

### 숫자 표현 원칙

- **크고 굵게**: 핵심 지표(물량, 가격, 개수)는 본문보다 크고 굵게
- **단위 분리**: 숫자와 단위는 시각적으로 구분 (크기/색상 차이)
- **정렬**: 숫자는 가능한 우측 정렬 또는 중앙 정렬
- **천 단위 구분**: 콤마(,) 또는 공백으로 가독성 확보

### 색상 적용

| 배경 | 텍스트 색상 |
|------|-------------|
| **어두운 패널** | `text-white`, `text-white/60` |
| **밝은 모달 내부** | `text-black`, `text-slate-600` |
| **입력 필드** | `text-black` (배경이 밝은 경우) |

---

## 5. 컴포넌트 시각 규칙

### 버튼

#### Primary Button (검색/주요 액션)
- **배경**: 청록 그라데이션 `linear-gradient(135deg, #00F0FF 0%, #00B8C4 100%)`
- **텍스트**: 흰색, bold
- **글로우**: 활성 상태에서 발광
- **형태**: 각진 모서리 또는 약간의 라운드

#### Secondary Button (보조 액션)
- **배경**: 투명 또는 `rgba(255, 255, 255, 0.1)`
- **텍스트**: 흰색 또는 white/70
- **보더**: 1px white/30

#### Tab Button
- **비활성**: `text-white/50`
- **활성**: `text-white` + 하단 보더 강조
- **호버**: `text-white/80`

### 입력 필드 (Input)

#### 모달 내부 (밝은 배경)
- **배경**: 흰색
- **텍스트**: 검정 (`text-black`)
- **보더**: `border-slate-300`
- **포커스**: `ring-2 ring-teal-500`

#### 패널 내부 (어두운 배경)
- **배경**: `rgba(22, 22, 32, 0.8)`
- **텍스트**: 흰색
- **보더**: `2px solid rgba(255, 255, 255, 0.5)`

### 모달

- **오버레이**: `rgba(0, 0, 0, 0.5)` + `backdrop-blur`
- **패널 배경**: 흰색 또는 밝은 회색
- **헤더**: 어두운 배경 + 흰색 텍스트 (시스템 느낌 유지)
- **본문**: 밝은 배경 + 검정 텍스트 (가독성)
- **닫기 버튼**: 흰색/투명, 호버 시 밝아짐

### 테이블 / 리스트

- **헤더 행**: 어두운 배경, 흰색 텍스트, 약간 작은 폰트
- **데이터 행**: 밝은 배경, 검정/회색 텍스트
- **호버**: 행 전체 배경 미세 변화
- **구분선**: 얇은 회색 선 또는 간격으로 구분

### KPI 카드 (수치 패널)

- **숫자**: 크고 굵게, 중앙 또는 우측 정렬
- **단위**: 숫자보다 작고 연한 색
- **라벨**: 상단 또는 하단에 작은 텍스트
- **배경**: 반투명 어두운 패널
- **강조**: 중요 수치에 글로우 또는 색상 포인트

---

## 6. 모션 / 상태 표현

### Hover 상태

- **패널/카드**: `active:scale-[0.98]` - 살짝 눌리는 피드백
- **버튼**: 밝기 증가 또는 글로우 강화
- **텍스트 링크**: 투명도 변화 (white/60 → white)
- **시간**: `transition-all` 150-200ms

### Active 상태

- **탭**: 하단 보더 + 텍스트 밝기 최대
- **토글**: 배경색 변화 + 글로우
- **선택된 항목**: 보더 색상 변화 또는 배경 하이라이트

### Loading 상태

- **스피너**: 청록색 회전 애니메이션
- **스켈레톤**: 어두운 패널에 pulse 애니메이션
- **진행 표시**: 청록색 프로그레스 바
- **분위기**: "시스템 처리 중" 느낌 유지, 귀여운 애니메이션 금지

### 특수 애니메이션

- **마커 등장**: bounce 효과 (translateY 기반)
- **카운터**: 숫자 롤링/슬롯 애니메이션
- **LIVE 표시**: `animate-pulse` (초록색 점)

---

## 7. 금지 규칙

### 사용 금지 요소

| 요소 | 이유 |
|------|------|
| **컬러풀 그라데이션** | 청록 단색 그라데이션만 허용, 무지개/다색 금지 |
| **일반 박스 그림자** | 글로우만 사용, `box-shadow: 0 4px 6px` 스타일 금지 |
| **과도한 라운드** | `rounded-2xl` 이상 금지 (카드 전체 둥글게 하지 않음) |
| **밝은 배경 패널** | 지도 위 오버레이는 반드시 어두운 반투명 |
| **귀여운/친근한 요소** | 이모지, 일러스트, 부드러운 아이콘 금지 |
| **불필요한 장식** | 기능 없는 선, 도형, 패턴 금지 |

### 스타일 일관성 규칙

- **한 화면에 두 가지 보더 스타일 혼용 금지**: 청록 보더 또는 흰색 보더 중 하나로 통일
- **텍스트 색상 혼란 금지**: 같은 맥락에서 청록/흰색/회색 텍스트 무분별 혼용 금지
- **글로우 남용 금지**: 한 화면에 글로우 요소 3개 이하 권장

### 예외 허용 케이스

- **모달 내부**: 가독성을 위해 밝은 배경 + 검정 텍스트 허용
- **지도 마커**: 구분을 위한 다양한 색상 허용
- **상태 표시**: 시맨틱 컬러(성공/경고/오류) 사용 허용

---

## 적용 가이드

이 문서를 기반으로 main 브랜치에 디자인을 적용할 때:

1. **공통 스타일 변수 정의**: tailwind.config.js에 컬러/폰트 확장
2. **레이아웃 컨테이너 스타일링**: CommandLayout, ServiceConsole 외곽 패널
3. **공통 컴포넌트 업데이트**: Button, Input, Modal, Card 등
4. **페이지별 세부 조정**: 각 탭/섹션의 잔여 스타일 보정

---

*이 문서는 코드가 아닌 "시각적 원칙"을 정의한다. 구현 시 이 원칙을 해석하여 적용한다.*
