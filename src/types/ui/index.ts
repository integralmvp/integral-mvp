/**
 * UI Types - UI 전용 Props/ViewModel 타입
 * (호환 레이어: types/models.ts에서 재export)
 */

export type {
  ProductCardProps,
  UnitLoadModule,
  HandlingOption,
  MatchStatus,
  MatchResult,
  CostEstimate,
  AreaInputType,
} from '../models'
